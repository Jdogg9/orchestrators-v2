version: "3.9"

services:
  orchestrators-v2:
    build: .
    image: orchestrators-v2:local
    env_file: .env
    ports:
      - "127.0.0.1:8088:8088"
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped
